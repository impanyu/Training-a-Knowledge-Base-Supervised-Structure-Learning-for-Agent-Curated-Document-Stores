\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{array}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,fit}
\usepackage{url}

% Float placement: the paper carries nine floats, and LaTeX's defaults are
% tight enough that they queue up and land at the end. Allow a page to be
% mostly floats and let more of them share one page.
\renewcommand{\topfraction}{0.92}
\renewcommand{\bottomfraction}{0.6}
\renewcommand{\textfraction}{0.06}
\renewcommand{\floatpagefraction}{0.72}
\renewcommand{\dbltopfraction}{0.92}
\renewcommand{\dblfloatpagefraction}{0.72}
\setcounter{topnumber}{3}
\setcounter{bottomnumber}{2}
\setcounter{totalnumber}{5}
\setcounter{dbltopnumber}{2}
\newcommand{\todo}[1]{\textcolor{red}{[TODO: #1]}}
\newcommand{\kbgym}{KBGym}

\begin{document}

\title{Training a Knowledge Base:\\Supervised Structure Learning for Agent-Curated Document Stores}

\author{\IEEEauthorblockN{Yu Pan}
\IEEEauthorblockA{\textit{University of Nebraska--Lincoln} \\
yu.pan@unl.edu}
\and
\IEEEauthorblockN{Hongfeng Yu}
\IEEEauthorblockA{\textit{University of Nebraska--Lincoln} \\
hfyu@unl.edu}}

\maketitle

\begin{abstract}
Retrieval-augmented generation treats the document store as a frozen input. A recent
wave of systems instead lets an LLM agent continuously curate a persistent knowledge
base, yet none of this work measures what curation does to the store itself. We invert
the usual framing: \emph{the knowledge base is the model}. A training agent consumes
supervised QA pairs one at a time---attempting each question against the current store,
receiving the gold answer, then consolidating verified reasoning into the store's
structure. An independent frozen-store reader is then
examined on held-out questions under a fixed action budget. Our central hypothesis is an \emph{efficiency} claim:
by materializing reasoning paths into index and shortcut structure, a trained store
lets an unchanged reader answer at \emph{non-inferior accuracy in fewer steps},
amortizing one-time training cost across all future queries. The protocol yields the
full apparatus of machine learning for knowledge bases: learning curves, a train/test
generalization gap, per-class transfer, and a break-even analysis of training cost
against per-question savings. On a contamination-free synthetic universe with
document-level ground truth (500 people, 5{,}864 atomic documents), two
training epochs lift \emph{re-encountered} questions from 0.59 to 0.85 F1
at 38\% fewer steps, with gains concentrated exactly in the relation-level
question classes the design predicts, and the links the agent builds join
documents that co-occur in a question's support set at 720$\times$ the
chance rate. On \emph{unseen} questions, however, the efficiency claim
fails: held-out accuracy is non-inferior to a flat store at two action
budgets but never cheaper ($\rho \approx 1$), and neither do
offline-construction baselines (GraphRAG- and HippoRAG2-style) save any
steps. A difficulty-stratified attribution locates the reason: built
structure pays where derivation is expensive and taxes retrieval where it
is not. We report the negative result with the apparatus that makes it
legible.
\end{abstract}

\begin{IEEEkeywords}
knowledge bases, LLM agents, retrieval-augmented generation, agentic RAG, memory, benchmarks
\end{IEEEkeywords}

\section{Introduction}
The dominant way to give language models durable knowledge is retrieval-augmented
generation (RAG): embed a corpus once, retrieve at query time, never write back. A
rapidly growing line of work breaks this asymmetry. Agent-maintained wikis compile
sources into evolving page networks~\cite{llmwiki-rar,llmwiki-stream,karpathy-llmwiki};
agentic memory systems accumulate and link notes across sessions~\cite{amem}; and
``sleep-time'' agents reorganize memory between conversations~\cite{letta-sleep}. The
pattern has reached practice ahead of measurement: the community's own description of
the wiki pattern lists a \emph{lint} step---checking contradictions, stale claims,
orphan pages, missing cross-links---as an engineering habit with no formal evaluation
attached~\cite{karpathy-llmwiki}, and the closest published systems evaluate only
downstream answer accuracy, never the store~\cite{llmwiki-rar,llmwiki-stream}.

We study the store directly, under the oldest framing machine learning has:
\emph{train/test}. The knowledge base is the model. Supervised questions arrive one at
a time; a training agent answers each from the current store, is graded, and then, in
an explicit consolidation phase, edits the store's structure so that the question's
\emph{class} becomes easier for a future reader. Evaluation freezes the store and
examines an independent reader with no gold access and a fixed action budget on
held-out questions.

The value produced by such training is best understood as \emph{index construction}:
a database does not change its data when an index is built, yet queries get cheaper.
Each training question's verified reasoning path is materialized as
\emph{access structure}---a multi-hop chain becomes a traversable link
path, a key that search handles badly gains an index document linking
everything under it---so future readers pay a lookup price instead of a
re-derivation price. The distinction from writing the answer into a
document is the whole point: an answer serves the instance that produced
it, an index serves every question that enters through the same key. Training cost is thereby amortized: after a computable number of questions, the
one-time cost of curation is repaid by per-question savings.

\textbf{Contributions.}
\begin{enumerate}
\item \textbf{Agent-trained knowledge bases.} We propose letting an LLM agent
\emph{train} a document store under the standard machine-learning training
paradigm, with the knowledge base playing the role of the model: supervised
(question, answer) pairs are consumed one at a time over multiple epochs; each
iteration runs a forward pass (answer the question from the current store) and
a backward pass (with the revealed gold in hand, \emph{edit the store
itself}---adding, rewriting, deleting, and linking documents; building index
and navigation documents---so the question's class becomes easier for a future
reader); held-out performance is the loss. The knowledge base, not the model
weights, is the learned object, and the structure is carved incrementally by
the questions actually asked rather than induced from the corpus alone. In
machine-learning terms the contrast with offline graph construction
(GraphRAG, RAPTOR, HippoRAG) is exact: those are \emph{unsupervised} ---
structure inferred from the corpus with no labels and no notion of what will
be asked --- while ours is \emph{supervised}: (question, answer) pairs are
the labels, and structure is optimized against verified answers.
\item \textbf{A train/test protocol for knowledge bases}: two-phase
(forward/backward) training iterations, single-shot gold reveal,
frozen-store examination under action budgets, learning curves, and a
three-layer generalization decomposition (re-encountered instances / unseen
instances / unseen templates).
\item \textbf{\kbgym}, a contamination-free environment in the PhantomWiki
style~\cite{phantomwiki}: a fictional-person universe rendered as a graph of
atomic single-sentence documents; ten diagnostic question classes with exact
answers and support sets; deterministic, integer-exact store metrics; no LLM
judge anywhere.
\item \textbf{Cost as a first-class outcome}: accuracy, store size,
training cost, and per-question answering cost reported together, with a
break-even analysis of when training a store pays for itself.
\end{enumerate}

\section{Related Work}
\textbf{Retrieval-augmented generation and agentic retrieval.} Classic RAG
retrieves from a frozen corpus with a learned dense retriever and generates
conditioned on the results~\cite{rag-lewis,dpr,realm,fid}: the corpus is an
input, never an output. Agentic RAG moves retrieval into the reasoning loop
--- ReAct interleaves reasoning with tool calls~\cite{react}, IRCoT
interleaves chain-of-thought with retrieval~\cite{ircot}, Self-Ask
decomposes into sub-questions~\cite{selfask}, FLARE retrieves on low
generation confidence~\cite{flare}, Self-RAG learns when to retrieve and how
to judge what returns~\cite{selfrag}, and recent systems frame the
retrieve-or-reason decision as an MDP~\cite{deeprag} or train the search
policy with RL~\cite{searchr1,agentic-rag-survey}. Our reader \emph{is} such
a loop. The distinction is which side learns: that line improves the
query-side policy against a fixed corpus; we hold the policy fixed and train
the store. The axes are orthogonal and composable.

\textbf{Building structure over a corpus.} GraphRAG~\cite{graphrag},
RAPTOR~\cite{raptor}, LightRAG~\cite{lightrag}, and
HippoRAG~\cite{hipporag,hipporag2} build entity graphs, summary hierarchies,
or PageRank-linked knowledge graphs \emph{before} any question arrives.
These are our strongest baselines and the natural contrast: their structure
is unsupervised and built blind to the query distribution, ours is carved
incrementally by the questions actually asked. On the parametric side, ROME
and MEMIT edit factual associations inside model weights~\cite{rome,memit};
we pursue the non-parametric complement --- facts live in an external store
that can be inspected, reorganized, and audited, with edits that transfer
across models. STaR-style bootstrapping~\cite{star} keeps only verified
reasoning for further training; our forward/backward iteration inherits that
predict-then-learn shape, with the store rather than the weights as the
learned object.

\textbf{Agent-maintained stores and agent memory.} Self-evolving LLM-wiki
retrieval~\cite{llmwiki-rar} has an agent write and update wiki pages during
question answering; streaming knowledge compilation~\cite{llmwiki-stream}
maintains a time-evolving wiki under a document stream; STORM~\cite{storm}
generates Wikipedia-style articles by multi-perspective research --- one-shot
authorship rather than continual maintenance. A parallel line accumulates
experience across episodes: MemGPT pages context in and out of external
memory~\cite{memgpt}; Mem0 and A-MEM maintain linked long-term note
stores~\cite{mem0,amem}; generative agents retrieve and reflect over an event
stream~\cite{generative-agents}; Voyager grows a skill library~\cite{voyager};
Reflexion, ExpeL, and Agent Workflow Memory distill verbal feedback,
cross-task insights, and reusable workflows~\cite{reflexion,expel,awm};
Dynamic Cheatsheet and ACE evolve a test-time strategy memory or context
playbook~\cite{cheatsheet,ace}. In these the artifact is a prompt or a
private memory serving one agent; ours is a shared, inspectable document
store whose curation quality is itself the measured outcome.

\textbf{Measuring memory without contamination.} The published curation
systems report downstream task scores only: the store itself is never
measured, there is no train/test split over questions, and the evaluation
corpora are parametrically contaminated. LongMemEval and successors probe
assistant memory with QA over past sessions~\cite{longmemeval};
LoCoMo evaluates very-long-term conversational memory~\cite{locomo};
MemDelta documents hidden confounds and argues for controlled
baselines~\cite{memdelta}, supporting our no-store baseline discipline.
Multi-hop QA datasets --- HotpotQA~\cite{hotpotqa}, MuSiQue~\cite{musique},
2WikiMultiHopQA~\cite{twowiki} --- supply real-text questions with annotated
support, but their public corpora are contaminated for current models.
PhantomWiki~\cite{phantomwiki} generates contamination-free fictional
universes with programmatically exact answers; our generator follows its
recipe, and SQuAD-style normalization~\cite{squad} provides the grading.
Our protocol adds the missing measurement layer.

\definecolor{okblue}{HTML}{0072B2}
\definecolor{okorange}{HTML}{E69F00}
\definecolor{okverm}{HTML}{D55E00}
\definecolor{okgreen}{HTML}{009E73}
\definecolor{okpurple}{HTML}{9A5EA8}
\definecolor{okteal}{HTML}{56B4E9}

\begin{figure*}[t]
\centering
\begin{tikzpicture}[
  font=\small,
  node/.style={circle, draw=okblue!80, fill=okblue!25, inner sep=1.6pt},
  nav/.style={circle, draw=okverm, fill=okverm!85, inner sep=2.2pt},
  stage/.style={draw, rounded corners=2pt, align=center, inner sep=5pt},
  lbl/.style={font=\footnotesize\itshape, text=black!70},
  arr/.style={-{Stealth[length=2.2mm]}, thick, draw=black!65},
  >=Stealth]

% ---- left: initial store
\node[stage, draw=okblue!70, fill=okblue!5, minimum width=3.4cm,
      minimum height=2.5cm] (K0) {};
\node[above=1mm of K0.north, anchor=south] {\textbf{initial store} $K_0$};
\node[lbl, below=0mm of K0.south, anchor=north] {atomic documents, no links};
\foreach \i/\x/\y in {1/-1.1/0.6, 2/-0.3/0.8, 3/0.6/0.55, 4/-1.05/-0.1,
                      5/0.1/0.05, 6/1.05/0.0, 7/-0.6/-0.7, 8/0.35/-0.75, 9/1.0/-0.65}
  \node[node] (a\i) at ([shift={(\x,\y)}]K0.center) {};

% ---- middle: training loop
\node[stage, draw=okorange!85, fill=okorange!6, right=1.5cm of K0, minimum width=4.2cm, align=left] (train) {
  \textbf{training iteration} $(q, a^{*})$\\[1pt]
  \textcolor{okblue!80!black}{\emph{Phase 1} forward ($N_1$): search/read $\to$ \texttt{answer}}\\
  \hspace{2mm}$\hookrightarrow$ result reveals $a^{*}$, F1\\[1pt]
  \textcolor{okverm!90!black}{\emph{Phase 2} backward ($N_2$): add/edit/delete/}\\
  \textcolor{okverm!90!black}{\hspace{2mm}link/unlink $\to$ \texttt{done}}
};
\node[lbl, below=0mm of train.south, anchor=north]
  {$\times$ 150 questions $\times$ epochs};

% ---- right: trained store
\node[stage, draw=okgreen!80, fill=okgreen!5, right=1.5cm of train, minimum width=3.4cm, minimum height=2.5cm] (KT) {};
\node[above=1mm of KT.north, anchor=south] {\textbf{trained store} $K_T$};
\node[lbl, below=0mm of KT.south, anchor=north] {links + navigation documents};
\foreach \i/\x/\y in {1/-1.1/0.6, 2/-0.3/0.8, 3/0.6/0.55, 4/-1.05/-0.1,
                      5/0.1/0.05, 6/1.05/0.0, 7/-0.6/-0.7, 8/0.35/-0.75, 9/1.0/-0.65}
  \node[node] (b\i) at ([shift={(\x,\y)}]KT.center) {};
\node[nav] (hub) at ([shift={(-0.2,-0.35)}]KT.center) {};
\draw[okgreen!70!black, thick] (hub)--(b4) (hub)--(b5) (hub)--(b7) (hub)--(b8)
                (b2)--(b5) (b3)--(b6) (b6)--(b9);

% ---- arrows
\draw[arr] (K0) -- node[above, lbl]{} (train);
\draw[arr] (train) -- (KT);

% ---- bottom: exam
\node[stage, draw=okpurple!80, fill=okpurple!6, below=0.9cm of KT, minimum width=4.6cm, align=center] (exam)
  {\textbf{exam} (store frozen)\\ fresh reader: search/read/\texttt{answer},
   budget $M$\\ held-out questions, gold never shown};
\draw[arr] (KT) -- (exam);
\node[stage, draw=okpurple!80, fill=okpurple!6, left=1.2cm of exam, align=center] (out)
  {F1 (non-inferior?)\\ steps per question ($\rho<1$?)};
\draw[arr] (exam) -- (out);
\end{tikzpicture}
\caption{The train/test protocol. A training agent consumes supervised QA pairs,
answering each from the current store (gold revealed only after its single answer)
and consolidating verified reasoning into structure. Evaluation freezes the store and
examines an independent budgeted reader on held-out questions; the hypothesis
(Eq.~\ref{eq:hypothesis}) is non-inferior accuracy at strictly lower per-question
cost.}
\label{fig:protocol}
\end{figure*}

\section{Problem Formulation}
A knowledge base $K$ is a directed graph of \emph{documents}. A document is
deliberately small --- one self-contained sentence and its outgoing edges ---
so that what the store learns is visible in its structure rather than hidden
inside prose, and so that every edit is attributable.

Two fields are all any agent ever sees or writes. \texttt{text} is one
sentence stating one fact, with full names and no pronouns: it is the only
thing \texttt{search} matches against and the only thing a reader is shown,
so a document that packs two facts is retrievable by neither cleanly.
\texttt{links} are outgoing edges to other documents; \texttt{read} returns a
document together with the full text of everything it links, which is what
lets a well-connected document replace a sequence of uncertain searches. An
\emph{index document} is not a distinct type but a document whose text names
a key and whose worth lies in its links rather than in the fact it states ---
nothing in the store marks it as one, which is why an index carrying no links
is indistinguishable from a sentence nobody needs.

The environment additionally keeps three fields the agent can neither read
nor set, so that measurement does not depend on its cooperation:
\texttt{origin} records which original fact a document represents (empty when
the agent wrote it), \texttt{flag} distinguishes untouched originals from
authored and edited ones, and \texttt{absorbed} records the origins folded in
when a duplicate was merged. Together they make the ledger exact: every
original fact is either still represented or demonstrably lost, and every
piece of structure the agent added is separable from the corpus it was given.

The \emph{reader} is a fixed
LLM policy with three tools---vector \texttt{search}, document \texttt{read} (text plus
linked documents), and \texttt{answer}---a fixed action budget $M$, and a short-term
memory holding only the current question and the last $k$ action--result pairs.
Reader performance on a question distribution $Q$ defines store quality along two
axes:
\begin{align*}
\mathrm{acc}(K) &= \mathbb{E}_{q\sim Q}\!\left[\mathrm{F1}\big(\mathrm{reader}(K,q)\big)\right],\\
\mathrm{cost}(K) &= \mathbb{E}_{q\sim Q}\!\left[\mathrm{steps}\big(\mathrm{reader}(K,q)\big)\right].
\end{align*}
\emph{Training} is any procedure that consumes supervised pairs $(q, a^{*})$ and edits
$K$, producing $K_T$ from the initial store $K_0$. What such a procedure is
meant to learn is the store's \emph{access structure}, and it is worth
being precise about this, because the action set admits a shortcut that
looks like learning and is not. A store can be changed in two ways: by
adding \emph{content} --- a document stating a fact, including a fact the
store already implies, such as the answer to a question just solved --- or
by adding \emph{access structure}: index documents whose value lies in the
links they carry to the documents belonging to some key, and edges between
documents that a chain must step across (including edges between index
documents, which give navigation its levels). Only the second generalizes.
A document recording a verified answer serves the one instance that
produced it, and the instance will not recur; an index over a key serves
every question whose first search names that key, and an edge serves every
chain that must make that hop, whatever entity it concerns. Our hypothesis
is therefore a claim about access structure specifically, and we measure
the store's navigability --- index out-degree and the fraction of content
reachable from an index in one read --- alongside accuracy and cost. Our central hypothesis is a joint
condition on the two axes:
\begin{equation}
\mathrm{acc}(K_T) \;\ge\; \mathrm{acc}(K_0) - \delta
\quad\text{and}\quad
\rho \;=\; \frac{\mathrm{cost}(K_T)}{\mathrm{cost}(K_0)} \;<\; 1,
\label{eq:hypothesis}
\end{equation}
with non-inferiority margin $\delta = 0.03$. Training cost amortizes: if training
spent $C_{\mathrm{train}}$ tokens and the per-question saving is
$c_0 - c_T$ tokens, the \emph{break-even point}
\begin{equation}
N^{*} \;=\; \frac{C_{\mathrm{train}}}{c_0 - c_T}
\label{eq:breakeven}
\end{equation}
is the query volume after which curation has paid for itself---the single number
that tells a practitioner whether training a store is worth it for their workload.

The correspondence to supervised learning is exact: the store configuration is the
parameter vector, one training iteration is one optimization step, the training agent
is the optimizer, and held-out reader performance is the loss. Formally, the
environment with a \emph{fixed} reader policy induces a Markov process over store
configurations; training is a guided walk through configuration space, and the
questions are its supervision signal. Because training-agent forward passes and test
readers use identical tools, budgets, and the single-shot answer rule, the train/test
generalization gap is well defined: the three-layer decomposition compares accuracy on
re-encountered train instances (retention carried \emph{only} by the store---the
agent's memory resets every question), unseen instances of trained templates
(class-level generalization), and reserved templates (within-category transfer).

\section{The \kbgym{} Environment}\label{sec:env}
\subsection{Universe and Store}
A generator in the PhantomWiki style samples a fictional population (500 people in
the main configuration): family trees, spouses, friendships, per-person attributes
(job, hobby, city), globally distinct birthdates, and \emph{name distractors}---extra
people sharing first names with questioned subjects. Facts render through fixed
templates into $\sim$5{,}900 atomic single-sentence documents (``Alice Johnson's job is
arborist.''). The initial store has \emph{zero links}: a bag of atomic facts.
Document embeddings back \texttt{search} and are maintained
automatically as documents are added, edited, or deleted.

\subsection{Actions}\label{sec:actions}
Table~\ref{tab:actions} is the complete action set. Availability is enforced
by the tool schema rather than at runtime: the reader is never shown an
editing tool, so it cannot decline to use one, and the curator is never
shown \texttt{answer}. The reader's three actions are byte-identical in
training phase~1 and at exam time, which is what makes the frozen store the
only thing that differs between the two.

Two prices are set deliberately. \texttt{search} returns five documents per
page and a page costs an action, so enumerating a set is possible but
expensive --- the twenty-nine residents of a city cost six actions, while one
\texttt{read} of a complete index costs one. Closing that gap is exactly
what a trained store is for, and returning sixty results at once would erase
the thing being measured. Conversely \texttt{link\_many} attaches any number
of targets for a single action, so building an index is cheap once its
members are found: the curator's budget goes on finding, not on attaching.
Batches above forty targets are rejected --- the largest genuine key in this
universe has thirty-six members, so a larger batch is a search result rather
than a set, and truncating it silently would leave the curator believing it
had built something it had not.

Provenance is tracked throughout: initial documents carry an origin, edited
ones are flagged, authored ones have no origin. Coverage and duplication are
therefore integer-exact and any authored content is attributable.

\begin{table}[!t]
\caption{The action set. \emph{R} = available to the reader (exam and
training phase~1); \emph{C} = available to the curator (training phase~2).
No action is available to both roles in a way that lets the reader modify
the store.}
\label{tab:actions}
\centering
\scriptsize
\renewcommand{\arraystretch}{1.15}
\setlength{\tabcolsep}{4pt}
\begin{tabular*}{\columnwidth}{@{\extracolsep{\fill}}lL{4.3cm}cc@{}}
\toprule
Action & Effect & R & C \\
\midrule
\texttt{search(q, page)} & five most similar documents; one page per action & \checkmark & \checkmark \\
\texttt{read(id)} & the document and the full text of everything it links to, one level & \checkmark & \checkmark \\
\texttt{answer(text)} & submit and end the pass & \checkmark & \\
\midrule
\texttt{add(text)} & new document; a near-duplicate is merged instead & & \checkmark \\
\texttt{edit(id, text)} & replace a document's text & & \checkmark \\
\texttt{delete(id)} & remove a document & & \checkmark \\
\texttt{link(a, b)} & one directed edge & & \checkmark \\
\texttt{link\_many(a, T)} & up to forty edges from \texttt{a} for one action & & \checkmark \\
\texttt{unlink(a, b)} & remove an edge & & \checkmark \\
\texttt{done()} & end the curation pass & & \checkmark \\
\bottomrule
\end{tabular*}
\end{table}

\subsection{Question Classes and Splits}
Ten categories over $\sim$26 templates. \emph{Forward} classes QC1--QC3 (1--3-hop
named-entity chains) are natively easy for document-level retrieval and serve as the
non-inferiority floor. \emph{Hard} classes target relations \emph{between}
documents: aggregation counts (QC4, QC8), abstention (QC5), multi-constraint joins
without a name anchor (QC6), set intersection (QC7), comparison/superlatives over
birthdates (QC9), and reverse lookup with a uniqueness guarantee (QC10). Golds are
computed by graph traversal over the fact layer; every question carries template id,
gold variants, and its support set. Splits (instance-disjoint): \texttt{train} (150;
i.i.d.\ over all categories, 60\% hard), \texttt{test\_in} (100; unseen instances of
trained templates), \texttt{test\_out} (50; one reserved template per category),
\texttt{eval} (30; stratified, drives the per-epoch learning curve so the final test
is touched exactly once). Grading is SQuAD-normalized F1/EM.

\subsection{Design Rationale}\label{sec:design}
Four decisions carry the methodology. \emph{(i) No LLM judge anywhere}: golds are
exact, grading is SQuAD normalization, store metrics are integer counts over
provenance---every number in this paper is reproducible to the digit.
\emph{(ii) Realistic retrieval}: \texttt{search} returns matched document text, as
production vector stores do; an early variant returning only titles collapsed the
reader to F1 $0.0$ and would have credited the trained store for fixing an
artificially crippled interface. \emph{(iii) Atomic documents}: the store is its
own chunking, so organization is expressible \emph{only} through links and navigation
nodes---any efficiency gain is attributable to structure, and any authored content is
provenance-flagged. \emph{(iv) Contamination control}: we measured gpt-5-mini
answering HotpotQA-style questions at F1 $\approx 1.0$ with \emph{no retrieval at
all}; on public corpora, a reader's score conflates the store with the weights, and
``store value'' loses meaning. A fictional universe makes the no-store score
$\approx 0$, so every point of reader performance is earned through the store.

\section{Training Protocol}
\begin{algorithm}[t]
\caption{Training a knowledge base (reader and editor tool sets as defined
in Sec.~\ref{sec:actions})}
\label{alg:train}
\begin{algorithmic}[1]
\State evaluate reader on \texttt{eval} \Comment{epoch-0 baseline (= B1)}
\For{epoch $= 1 \dots E$}
  \For{$(q, a^{*})$ in shuffled \texttt{train}}
    \State reset short-term memory; task $\gets q$
    \For{step $= 1 \dots N_1$}  \Comment{forward}
      \State $d \gets$ policy(reader tools)
      \If{$d = \texttt{answer}(\hat a)$}
        \State reveal $a^{*}$, F1$(\hat a, a^{*})$; \textbf{break}
      \EndIf
    \EndFor
    \For{step $= 1 \dots N_2$}  \Comment{backward}
      \State $d \gets$ policy(editor tools)
      \If{$d = \texttt{done}$} \textbf{break} \EndIf
    \EndFor
    \State refresh dirty embeddings; log
  \EndFor
  \State snapshot; evaluate reader on \texttt{eval} \Comment{curve point}
\EndFor
\State exam: frozen store, test splits, budget $M$
\end{algorithmic}
\end{algorithm}

Phase 1 uses exactly the reader's toolset and budget, so train-forward accuracy is
directly comparable to test accuracy. The single-shot \texttt{answer} reveals the gold
in its result---standard supervised learning: predict, then see the label, then
update. Leftover Phase-1 budget is forfeited (no smuggling forward steps into
consolidation); a budget-exhausted forward scores $0$ but still receives the gold, so
failed questions---where repair matters most---get targeted consolidation.
Every intermediate store state is reconstructible by replaying the edit trace
(validated byte-exact against epoch snapshots), giving per-iteration store
trajectories for analysis at no storage cost.

What the backward pass is told is deliberately uniform: no oracle
localization, and no branching on how the forward pass failed. The agent
receives its own trajectory, the gold answer, its F1, and one of three
coarse outcomes (budget exhausted, answered wrongly, answered correctly),
followed by the same instruction in every case --- name the keys this
question mentioned, the people, places, jobs, hobbies and relations it
named, and make sure each has a complete index document: build what is
missing, extend what is partial, change nothing if they are already
complete.

The curation skill supplies the standing constraints rather than a
procedure. An index \emph{points}; its members belong in its links, never
recited in its sentence, so the index survives the facts changing under it.
One key at a time: a question naming four keys and a budget that closes one
should close one, because an index of twenty-nine members is worth more than
four of three, and an unreached key is picked up by the next question naming
it. Precision before completeness: search returns what is similar, not what
belongs, so linking a whole result set destroys the certainty an index
exists to provide. Completeness is then a promise --- a reader that finds an
index stops searching, so a partial index does not merely underperform, it
makes the reader confidently count nine where there are fourteen. And an
index under which the store holds nothing is deleted rather than left
standing, since it would cost a retrieval slot and return nothing.
Parsimony governs all of it: duplicated documents compete in search and bury
each other. That last constraint encodes the lesson of preliminary runs on
an earlier environment variant, in which an agent trained \emph{without} it
quintupled an already-organized store and regressed the reader by 17 F1
points while every individual edit looked constructive.

Credit assignment needs no oracle: a wrong answer plus the revealed gold
lets the agent re-search \emph{with the answer in hand}, and what to repair
follows from the difference between where it searched and where the answer
turned out to live.

\section{Experimental Design}
\subsection{Benchmarks}
Three arms share one protocol and differ in document granularity and realism
(Table~\ref{tab:arms}).

\begin{table}[t]
\caption{Benchmark arms. All use the same splits scheme, reader, and budgets.}
\label{tab:arms}
\centering
\scriptsize
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{L{1.35cm}L{1.75cm}L{1.75cm}L{1.75cm}}
\toprule
 & \kbgym{} (main) & PhantomWiki (official) & MuSiQue (real text) \\
\midrule
documents & 5{,}864 sentences & 3{,}403 sent.\ or 405 articles & $\sim$11k paragraphs \\
source & our generator & their generator & Wikipedia \\
categories & 10 (26 templates) & 4 shapes & 2--4-hop shapes \\
questions & 330 (150/100/50/30) & 330, same scheme & same scheme \\
supports & exact & unavailable & official \\
contamination & none & none & delta-corrected \\
status & built; main runs & data built & planned \\
\bottomrule
\end{tabular}
\end{table}

\textbf{\kbgym{} (main).} 500 fictional people (10\% name distractors; family
trees, spouses, friendships; job/hobby/city attributes; globally distinct
birthdates) render into 5{,}864 atomic documents. Ten question categories over
26 templates: forward chains QC1--QC3; aggregation counts QC4/QC8 (children,
friends, grandchildren, attribute-filtered counts); abstention QC5;
multi-constraint joins QC6 (no name anchor; unique answer enforced against
distractors); set intersection QC7 (common friends); comparison/superlatives
QC9 (birthdates; ties excluded by construction); reverse lookup QC10 (exactly
one qualifier). Golds are graph-derived with accepted variants; every question
carries its support set. Splits are instance-disjoint: \texttt{train} 150
(i.i.d.\ over all categories, 60\% hard), \texttt{test\_in} 100 (unseen
instances of trained templates), \texttt{test\_out} 50 (one reserved template
per category), \texttt{eval} 30 (stratified; drives the learning curve so the
final test is touched exactly once).

\textbf{Official PhantomWiki (external validity).} A generator we do not
control (phantom-wiki 1.0.3, SWI-Prolog 10.0.2): 16 family trees, 405 person
articles, 480 generated QA over 8 templates grouped into four composition
shapes, with per-question hop depth 1--5; two reserved templates (relational
and aggregative) form \texttt{test\_out}. Their Prolog-derived relations have
no per-fact supports, so repair diagnostics are unavailable on this arm; the
train/test protocol itself does not need them. Documents at sentence or
article granularity (the latter tests the method at paragraph scale with zero
contamination).

\textbf{MuSiQue (real-text arm)~\cite{musique}.} Paragraphs are the documents; the official
composition shapes are the categories, a held-out shape is
\texttt{test\_out}, and the annotated supporting paragraphs restore support
diagnostics. Real corpora are parametrically contaminated (we measured
near-ceiling no-retrieval scores for related datasets), so all scores on this
arm are reported as deltas over a mandatory no-store reader baseline.

\subsection{Baselines}
All baselines are \emph{unsupervised}: structure is induced from the corpus
alone, with no access to questions or answers---the defining contrast with
our trainer, for which gold answers are labels and the structure built is
whatever those labels showed to be missing. Beyond that they differ
\emph{only} in how the store is prepared; reader, test
sets, and budgets are identical, and each reports its one-time build cost
(Table~\ref{tab:baselines}).

\begin{table}[t]
\caption{Baseline arms on \kbgym{}-500. Identical reader, test sets, and
budgets; arms differ only in store preparation.}
\label{tab:baselines}
\centering
\scriptsize
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{L{1.62cm}L{1.5cm}L{2.15cm}L{1.3cm}}
\toprule
Arm & Preparation & Structure added & Build cost \\
\midrule
B1 flat & none & --- & 0 \\
B2 GraphRAG & offline, unsup. & 135 summary docs, 8.5k links & 488k tok \\
B3 HippoRAG2 & offline, unsup. & 660 index docs, 196k links & $\approx$0 \\
Ours & QA-supervised & links + nav docs & training run \\
\bottomrule
\end{tabular}
\end{table}
\textbf{B1, flat store}: the untrained store (= epoch~0). \textbf{B2,
GraphRAG-style}~\cite{graphrag}: entities extracted per document (lexical
extraction, justified by template-shaped text), co-occurrence communities,
one LLM-written community summary document per community linked to its
members. \textbf{B2, HippoRAG-2-style}~\cite{hipporag2}:
(subject, relation, object) triples per document, entity-sharing links
between documents, per-entity hub documents. B2 and B2 structure lands as
authored documents and links \emph{in our store format}, so the common
budgeted reader can traverse it: we test their \emph{structure} under a fixed
reader rather than their retrieval algorithms (a documented adaptation; e.g.\
B3 forgoes Personalized PageRank).

\subsection{Implementation}
\textbf{Storage.} The store is held in memory as a dictionary from document
id to a record of \texttt{text} and a list of outgoing link ids; there is no
database and no object store. Links are ids only, never copies, so a document
is named in exactly one place and a delete cascades: the removed id is
stripped from every other document's link list rather than left dangling. The
whole store serialises to newline-delimited JSON, one object per document,
which is what the per-epoch snapshots contain and what makes them readable
and diffable.

\textbf{Retrieval.} A separate in-memory vector index (chroma, ephemeral
client, default ONNX MiniLM embeddings) serves \texttt{search} and nothing
else: it holds each document's \texttt{text} and id, and never its links or
provenance. Adding or editing a document marks it dirty; the environment
re-embeds every dirty document in one batch at the end of the iteration, and
a delete drops its row. The agent therefore never manages embeddings, and the
index is strictly derived state --- discarding it and rebuilding from the
documents changes nothing.

\textbf{Reproducibility.} Every action and its result is appended to a trace,
so any intermediate store can be rebuilt exactly by replaying that trace from
the initial universe, without an API call --- including merges, whose
verdicts are recorded rather than recomputed. Embeddings are not serialised
because they are recomputable from text. This is not only an analysis
convenience: when an API outage killed a run 100 iterations in, the store was
reconstructed from its own trace in under two minutes and training continued
from that point.

\textbf{Models and budgets.} Trainer and reader are gpt-5-mini (temperature
0.3, low reasoning effort, 12k completion tokens per turn), tool-forced
decisions, static schemas. The runs invoke the \texttt{gpt-5-mini} alias,
which resolved to snapshot \texttt{gpt-5-mini-2025-08-07}; we record the
snapshot rather than the alias because the alias will move, and the
duplicate judge (below) is the same model at minimal reasoning effort. Budgets $N_1{=}M{=}15$ and $N_2{=}20$; the
backward pass is longer because it must both locate a set and attach it. FIFO
memory $K{=}30$. \texttt{search} returns five documents by default and up to
sixty when asked, and \texttt{link\_many} attaches any number of documents in
one action, so the cost of building an index is dominated by finding its
members rather than by wiring them.

\textbf{Cost.} The 500-person main run averaged 37.7k tokens and 121\,s per
iteration (18.8M tokens, 16.4\,h, $\approx$\$13 at current pricing). Single
seed (seed~0); seed replication is left to future work. The infrastructure
deduplication guard is enabled: on add or edit the store embeds the candidate,
takes the three nearest documents, and consults an LLM judge when cosine
similarity exceeds $0.90$. The judge is given a completion budget of 500
tokens at minimal reasoning effort, which matters more than it sounds:
a reasoning model spends its budget thinking before it emits anything, and
at the four tokens an earlier version allowed, it returned an empty string
on every call --- read by the caller as ``not a duplicate'', so the guard
was wired up and inert. We measured this directly: 4 and 64 tokens both
yield empty output, 500 with minimal effort answers in about ten. Any
number reported here for merges therefore comes from a judge that actually
answered.

\subsection{Experiment Suite and Predictions}\label{sec:suite}
\textbf{E1 (headline).} Learning curve: eval-split F1 and steps after each
epoch (epoch~0 = B1); final \texttt{test\_in}/\texttt{test\_out} pass on the
last snapshot, at $M \in \{8, 15\}$. Prediction: non-inferior accuracy with
fewer steps, and a leftward shift of the accuracy--budget curve (the trained
store dominates at tight budgets). \textbf{E2.} The baseline table: F1,
steps/question, build tokens, per-question tokens, for B1/B2/B2/ours on
both test splits. \textbf{E3} (log slicing). Three-layer generalization
(train-forward from epoch~2 / \texttt{test\_in} / \texttt{test\_out}) and the
per-category profile. Prediction: forward classes QC1--QC3 are saturated near
1.0 at $M{=}15$ (they are the non-inferiority floor, not the headroom);
gains concentrate in aggregation (QC4/QC8), joins (QC6), intersection (QC7)
and reverse lookup (QC10), whose answers live in relations between documents;
QC5 abstention is expected unchanged. \textbf{E4} (trace mining). (i)
edit-type mix per epoch; (ii) store-size trajectory from replay (the
parsimony objective predicts sublinear growth); (iii) morphology of authored navigation documents
(out-degree distribution, what they gather); (iv) structure--semantics
alignment: the share of built links connecting documents that co-occur in
some support set (supports are invisible to the agent, making the measure
honest); (v) mechanism attribution: correlation between a reader's step
savings and its hits on authored navigation documents and trained links.
Break-even $N^{*}$ (Eq.~\ref{eq:breakeven}) closes the analysis.

\section{Experiment Results}\label{sec:prelim}
All results below are from the final environment (Sec.~\ref{sec:env})
and the main run, keyed to the suite of Sec.~\ref{sec:suite}:
the calibration that fixed the main configuration
(Sec.~\ref{sec:res-calib}), then the learning curve (E1,
Sec.~\ref{sec:res-e1}), the key-coverage gradient that carries the main
result (E2, Sec.~\ref{sec:res-grad}), retention and its per-category
profile (E3, Sec.~\ref{sec:res-e3}), and trace mining (E4,
Sec.~\ref{sec:res-e4}).

\subsection{Calibration and Cost Profile}\label{sec:res-calib}
Reader difficulty was calibrated by probes on untrained stores: forward named-entity
chains are natively easy for document-level retrieval at any tested scale (F1
$1.0$); headroom concentrates in the relation-level classes and grows with universe
size (untrained F1 $0.83$ at 120 people vs.\ $0.67$ at 500, mean steps $6.7$ vs.\
$9.2$)---motivating the 500-person main configuration, where the cost axis has
roughly $3\times$ headroom over the $\sim$3-step floor. Accuracy can
saturate while cost headroom persists---one reason efficiency is the
primary endpoint. The 300-iteration
main run consumed 18.8M training tokens over 16.4\,h ($\approx\$13$ at
current pricing).

\subsection{E1: Training Dynamics and the Learning Curve}\label{sec:res-e1}
The main run (500-person universe, full protocol; 300 iterations, 18.8M
training tokens, 16.4h) yields the training half of E1; the frozen-store
exam that completes it is reported below at $M{=}15$ and $M{=}8$. Two
training-time signals, then the exam.

\textbf{Held-out eval drifts up.} Eval-in $0.60 \to 0.60 \to 0.65$,
eval-out $0.50 \to 0.60 \to 0.60$ across epochs 0/1/2
($n{=}30$ per point, so these are directional).

\textbf{The store improves \emph{within} an epoch.} Rolling forward
accuracy climbs through epoch~1 (first 50 iterations $0.56$, last 50
$0.64$): later training questions already benefit from structure
consolidated by earlier ones, before any question repeats. This is the
evidence that the structure is genuinely accumulating rather than being
rebuilt per question: what earlier questions left behind is already paying
off before anything repeats.

\textbf{The frozen exam: accuracy transfers; efficiency does not.} An
unchanged reader on the frozen epoch-2 store is non-inferior to B1 at both
budgets, and at neither budget is it cheaper. At $M{=}15$ it scores
$0.630$/$0.660$ (\texttt{test\_in}/\texttt{test\_out}) vs.\ B1's
$0.620$/$0.620$ ($\Delta$ pooled $+0.020$; all $\Delta$ in this
paper are trained minus B1, the untrained flat store, on the same
questions), taking $8.73$ vs.\ $8.97$ steps
($\rho = 0.97$) and saving $80$ tokens per question. At $M{=}8$ it scores
$0.570$/$0.540$ vs.\ B1's $0.530$/$0.600$ ($\Delta$ pooled $+0.007$: better
on trained templates, \emph{worse} on reserved ones), at $6.23$ vs.\ $6.21$
steps ($\rho = 1.00$) and $194$ tokens \emph{more}. Both budgets clear the
non-inferiority margin ($-\delta = -0.03$ pooled), so the store the agent
built is not damaged---but the efficiency hypothesis, our central claim, is
\textbf{not supported on unseen questions}: $\rho \approx 1$ at both
budgets, break-even $N^{*} \approx 1.4 \times 10^{5}$ questions at $M{=}15$
and undefined at $M{=}8$, and the predicted leftward shift of the
accuracy--budget curve does not appear (the trained and flat curves are
within noise of each other at both points). The $-38\%$ step saving of
Sec.~\ref{sec:res-e3} is real but confined to \emph{re-encountered}
questions. Sections~\ref{sec:res-grad} and~\ref{sec:res-e4} locate the
gap: the saving tracks how much of a question's key the store indexed, and
the structure that was built pays only where derivation is expensive.

\begin{table}[!t]
\caption{Index construction quality, by the kind of key the index is built
on, with one example of each.}
\label{tab:idxquality}
\centering
\scriptsize
\renewcommand{\arraystretch}{1.1}
\setlength{\tabcolsep}{4pt}
\begin{tabular*}{\columnwidth}{@{\extracolsep{\fill}}lrrrr@{}}
\toprule
Key the index is built on & $n$ & deg. & prec. & recall \\
\midrule
attribute: city & 8 & 26.4 & 98\% & 95\% \\
\multicolumn{5}{@{}l}{\quad\emph{``Residents of Dorringham''}} \\[1pt]
attribute: hobby / job & 54 & 12.7 & 98\% & 96\% \\
\multicolumn{5}{@{}l}{\quad\emph{``People whose hobby is astronomy''}} \\[1pt]
single-entity hub & 91 & 8.0 & 95\% & 90\% \\
\multicolumn{5}{@{}l}{\quad\emph{``Bennett Coldwater''}} \\[1pt]
relation, two-hop & 10 & 3.9 & 87\% & 55\% \\
\multicolumn{5}{@{}l}{\quad\emph{``Grandchildren of Ivor Yarrow''}} \\[1pt]
relation, one-hop & 54 & 3.4 & 78\% & 73\% \\
\multicolumn{5}{@{}l}{\quad\emph{``Friends of Delphine Grimsby''}} \\
\midrule
all scored indexes & 217 & 8.5 & 94\% & 91\% \\
\bottomrule
\end{tabular*}
\end{table}

\subsection{E2: The Key-Coverage Gradient}\label{sec:res-grad}
\begin{figure}[!t]
\centering
\includegraphics[width=\columnwidth]{learning.pdf}
\caption{What training bought, as a function of how much of the question
the store had seen. Each curve is one question group ($n{=}10$) examined on
eleven frozen snapshots of the same run; the reader, the action set, the
budget and the questions are identical at every point, so the only thing
varying along the $x$-axis is the store. \emph{Trained on this question}
falls steadily; \emph{neither key seen} does not move. The two intermediate
groups say how far the effect reaches past the exact key.}
\label{fig:learning}
\end{figure}

[[GRADIENT TABLE AND ANALYSIS -- pending the probe, filling on completion]]

\subsection{E3: Retention and Its Per-Category Profile}\label{sec:res-e3}
\textbf{Retention is large, and concentrated exactly where predicted.}
On re-encountered questions (epoch~2) forward F1 reaches $0.85$
(vs.\ $0.59$ in epoch~1). Table~\ref{tab:retention} decomposes by category:
forward chains were saturated from the start (QC1--QC3, QC9 at
$0.86$--$1.00$ in both epochs --- retrieval alone suffices), while the
relation-level classes, whose answers live \emph{between} documents, jump:
joins QC6 $0.46 \to 1.00$, intersection QC7 $0.62 \to 1.00$, reverse lookup
QC10 $0.25 \to 0.83$, abstention QC5 $0.00 \to 0.58$, counts QC4
$0.17 \to 0.50$ and QC8 $0.46 \to 0.58$. This is the design prediction of E3
(Sec.~\ref{sec:suite}) realized: training gains concentrate in the classes retrieval
cannot solve natively.

\textbf{Retention is also cheaper, with a caveat about what is being
compared.} On re-encountered questions the forward pass takes $5.7$ steps
and 31k tokens, vs.\ $9.2$ steps and 45k tokens in epoch~1: $-38\%$ steps,
$-32\%$ tokens. Decomposing the cost, both terms move:
budget exhaustion falls from $47/150$ ($31\%$) to $7/150$ ($5\%$), and on
the 100 questions resolved within budget in \emph{both} epochs the lookup
itself shortens from $6.43$ to $4.64$ steps ($-28\%$) while F1 rises from
$0.870$ to $0.950$. Unlike the null result on unseen questions, then, the
conversion of derivation into lookup is genuine here. The caveat is that
these two epochs are not a controlled store contrast: the store evolves
\emph{during} epoch~1, so an early epoch-1 question meets a nearly
untrained store and a late one meets a partly trained store, and the epoch
label mixes store state with question order. The controlled version --- the
same training questions examined under the frozen initial store and the
frozen final store --- is the \emph{exact} row of the gradient table
(Sec.~\ref{sec:res-grad}).

\textbf{Three-layer decomposition, completed.} Retention $0.85$ /
\texttt{test\_in} $0.63$ / \texttt{test\_out} $0.66$. Both test layers
sit above B1, so the trained organization transfers accuracy --- but the
$0.2$-point gap to retention says most of the built structure is
instance-bound rather than class-general. The per-category transfer
profile ($\Delta$ vs.\ B1 at $M{=}15$, both splits pooled, same questions) is
correspondingly modest: QC4 $+0.10$, QC9 $+0.08$, QC3/QC7 $+0.07$, QC6
$+0.04$, QC5 $0.00$, QC8 $-0.05$, QC10 $-0.12$ ($n{=}8$). The sharpest
contrast is reverse lookup: the class with the largest retention jump
($0.25 \to 0.83$) transfers \emph{worst} --- its navigation documents
memoize trained instances, not the class.

\begin{table}[t]
\caption{Per-category forward F1: first encounter (epoch 1) vs.\
re-encounter (epoch 2). Retention concentrates in relation-level classes.}
\label{tab:retention}
\centering
\scriptsize
\renewcommand{\arraystretch}{1.15}
\setlength{\tabcolsep}{4pt}
\begin{tabular*}{\columnwidth}{@{\extracolsep{\fill}}L{2.5cm}rrr@{}}
\toprule
Category & $n$ & epoch 1 & epoch 2 \\
\midrule
QC1--QC3, QC9 (chains, compare) & 55 & 0.86--1.00 & 0.86--1.00 \\
QC6 join & 26 & 0.46 & \textbf{1.00} \\
QC7 intersection & 13 & 0.62 & \textbf{1.00} \\
QC10 reverse lookup & 12 & 0.25 & \textbf{0.83} \\
QC5 abstention & 12 & 0.00 & \textbf{0.58} \\
QC4 counts & 6 & 0.17 & \textbf{0.50} \\
QC8 deep counts & 26 & 0.46 & 0.58 \\
\bottomrule
\end{tabular*}
\end{table}

\begin{figure*}[!t]
\centering
\includegraphics[width=0.92\textwidth]{network.pdf}
\caption{The document network the agent built. \emph{Top:} the store at
three points in training; every document is drawn, and position is a t-SNE
projection of its embedding under the same function the store indexes with,
so documents drawn near each other are documents the reader's
\texttt{search} retrieves together (t-SNE preserves neighbourhoods, not
distances, so only local proximity should be read). One layout, computed
once on the final store and keyed by document, is shared by all panels, so a
document holds its position throughout and only the structure drawn over it
changes; the clusters visible at iteration~0 are the universe's natural
topic groups. Blue: source documents. Orange: authored index documents,
sized by degree. Green: links. The final panel (bottom right, at two thirds width) carries four
index documents labelled, together with the documents each one reaches
(purple). The four were selected for correctness, not size: every link each
of them carries points at a document that genuinely belongs under its key
(Sec.~\ref{sec:res-e4}), and together they span what the agent produced ---
an attribute index over a city, an attribute index over a hobby, a one-hop
relation, and a two-hop relation. Structure grows from nothing into a
navigation layer over the semantic space: hub indexes fan out into topic
clusters while local links join semantically adjacent documents.}
\label{fig:network}
\end{figure*}


\begin{figure}[!t]
\centering
\includegraphics[width=0.8\columnwidth]{trajectory.pdf}
\caption{(a)~What the store accumulates, reconstructed by trace replay.
\emph{Index documents} = agent-authored documents still alive; \emph{net
documents added} = store size minus the initial 5{,}864. The two curves
coincide: the agent adds access structure and essentially never rewrites
source documents, which is the parsimony objective realized (growth
$+4.9\%$ over 200 iterations). Link growth flattens after the epoch
boundary (dashed), the store saturating on the keys the training questions
name. (b)~Out-degree of every index document in the final store. The
distribution is long-tailed --- median~5, mean~6.7, max~33 --- with the
right tail carrying the attribute indexes of Table~\ref{tab:idxquality} and
a spike at zero: $43$ indexes ($15\%$) were opened and never filled.}
\label{fig:trajectory}
\end{figure}


\subsection{E4: Trace Mining---Edit Behavior and Store Morphology}\label{sec:res-e4}
\textbf{Where the structure lives.} Figure~\ref{fig:network} maps the
store's evolution across its semantic space (every document drawn; position
= t-SNE of the document embedding, so the clusters visible at iteration~0
are the universe's natural topic groups). Navigation documents appear
throughout the map; hub directories --- the dense orange cluster --- fan out
into the topic clusters, and short local links connect semantically adjacent
documents: the store visibly acquires an access layer it was born without.
Figure~\ref{fig:trajectory} quantifies the same growth from trace replay:
$+390$ documents ($+6.6\%$), 597 links, accumulating steadily across both
epochs --- the sublinear, structural growth the parsimony objective was
designed to enforce.

\textbf{Edit behavior.} The backward pass writes $\approx$1.6 documents and
$\approx$2.0 links per iteration in epoch~1; in epoch~2 the add rate falls
to $1.0$/iteration while linking stays at $2.0$ --- consistent with the
parsimony objective (less to author once the structure exists). Edits and
deletes stay rare ($0.13$ and $0.03$/iteration). The dedup judge fired
$\approx$30 times and correctly declined every candidate (zero merges):
prompt-level parsimony already suppressed duplication at this scale.

\textbf{Structure--semantics alignment: the links are real.} Of the 596
built links, 469 attach a navigation document (no support-set analogue
exists for those) and 127 connect two statements. Of those 127, $25.2\%$
join documents that co-occur in the support set of some universe
question---against a $0.03\%$ chance rate for random statement pairs, a
$720\times$ lift. Support sets are never visible to the training agent, so
this is an honest measure: when the agent links two facts, it is recovering
genuine reasoning adjacency, not surface similarity.

\textbf{Mechanism attribution: the observational split does not identify,
and we say so.} We had intended to attribute the reader's step savings to
its hits on built structure, splitting exam questions by whether the
trajectory touched an authored navigation document or a linked statement.
That split is not identifying, for a mechanical reason: touching is
\emph{downstream} of searching. The probability that a trajectory
encounters built structure rises from $0\%$ when the reader issues one
search to $63\%$ when it issues nine, so the touched group is selected for
long trajectories by construction. Difficulty matching does not repair
this: the touched group remains $+0.92$ steps worse than the untouched one
after exact stratification on B1's step count, which is exactly what
reverse causation predicts. Two secondary measurements rule out the
explanation we first reached for --- retrieval crowding. Authored documents
occupy $5.3\%$ of top-5 retrieval slots against a $5.7\%$ share of the
store by count, i.e.\ no over-retrieval, and their share \emph{rises} with
question difficulty ($2.2\%$ easy, $6.6\%$ hard), which is what relevance,
not pollution, looks like.

\textbf{What the agent actually wrote.} Inspection of the 284 authored
documents separates two kinds. Most ($80\%$) materialize an answer:
\emph{``Caleb Netherfield's father is Caleb Nightingale and Caleb
Nightingale's spouse is Petra Jessup and Petra Jessup's job is joiner''}
collapses a three-hop chain into one document; others aggregate a reverse
index (\emph{``People whose hobby is pottery include \ldots''}) or a count.
The remaining $20\%$ are pointer documents that state no fact at all ---
\emph{``Spouse job index gathers notes that record the jobs of spouses of
people''}, \emph{``A Junia Wetherby directory links to Junia Wetherby's
hobby, job, residence, friends, parent, and birth date notes''}. A reader
that retrieves one of these pays a slot and a read step and learns nothing,
then still has to find the facts. Notably the linking effort went to the
wrong kind: pointer documents carry $2.11$ links on average against $1.06$ for
contentful ones. Their absolute retrieval footprint is small ($0.9\%$ of
slots), so this is a design smell rather than a proven cost --- which is
precisely why the causal question needs an intervention on the store rather
than an observational split.

\textbf{What the structure reaches.} The action set can produce exactly two
kinds of structure: index documents that point at the documents belonging to
their key, and edges between documents that already exist (fact-to-fact, or
index-to-index, which is how navigation acquires levels). Profiling the
trained store by those classes says how far the built structure carries. The
284 authored indexes have mean out-degree $6.7$ (median $5$, max $33$); $43$
($15\%$) carry no links at all and $128$ ($45\%$) carry fewer than five. The
store holds $1{,}796$ index-to-fact edges, $100$ index-to-index edges and
$17$ fact-to-fact edges: the agent builds entry points far more readily than
it builds levels, so the navigation layer is broad and flat rather than
hierarchical. \textbf{$1{,}611$ of $5{,}864$ source documents ($27.5\%$) are
reachable from some index in a single read.} That figure is the binding
constraint on transfer: the reachable quarter is concentrated on the keys the
training questions named, which is exactly the population on which the
gradient of Sec.~\ref{sec:res-grad} shows a saving, and the unreachable
three quarters is why the saving does not survive a change of key.

\textbf{The indexes that do exist are accurate.} Table~\ref{tab:idxquality}
scores every index whose key can be resolved. A link counts as correct if it
points at a document about a genuine member of the key, resolved against the
universe rather than by string match: the grandchildren of a person are
recorded as ``\emph{X} is a child of \emph{Y}'' and never name the
grandparent, so a string-match test scores a correct two-hop index at zero,
and an earlier version of this analysis reported exactly that artifact.
Resolved properly, precision is $94\%$ and member recall $91\%$ across the
217 scorable indexes. Quality tracks the arity of the key rather than its
depth: attribute indexes over a city or a hobby are near-perfect ($98\%$
precision, $95$--$96\%$ recall) and carry the most links, while the
relational indexes are both smaller and noisier, and two-hop relations are
\emph{not} worse than one-hop ones ($87\%$ vs.\ $78\%$). Depth is not what
the curator struggles with; breadth is. The failure that remains is
silence rather than error --- the $43$ empty indexes are stubs like
\emph{``Delphine Thistlewood''} or \emph{``Felix Marchbanks is a person''},
opened by an agent that ran out of backward budget before filling them.

\textbf{The ablation that would settle it.} Holding reader, questions and
budget fixed and removing structure from the store gives the missing
counterfactual: the trained store minus its 284 authored documents ($17$
statement-to-statement links retained), and minus its links (authored
documents retained). We report the confound rather than the attribution it
would otherwise license.

\emph{Caveats: single seed; per-class $n$ as low as 6; epoch-2 ordering is
reshuffled, so per-class retention rides on small samples. The frozen-store exam separates transferable organization from instance memoization.}

\section{Discussion and Limitations}\label{sec:disc}
\textbf{Curation is a boundary, not a slogan.} Our results suggest
curation creates value where organization is lacking and pollution where it
is not: training helps on the disorganized main condition, while
GraphRAG-style summaries added to an already-searchable store regress
accuracy (Sec.~\ref{sec:res-grad}). The difference is carried by
whether the optimizer knows when \emph{not} to edit.
The train/test protocol makes this boundary measurable; downstream-only evaluation
cannot see a store being slowly ruined while individual answers still look fine.

\textbf{Reading the boundary.} \emph{What limits the method is coverage, not
construction.} Sec.~\ref{sec:res-e4} shows the indexes the agent builds are
accurate ($94\%$ precision, $91\%$ member recall), so the earlier reading ---
that the procedure simply fails to build usable structure --- does not
survive the final environment. What it does not do is build \emph{enough}:
only $27.5\%$ of source documents sit under any index, and that quarter is
the population the training questions named. The gradient of
Sec.~\ref{sec:res-grad} is the direct consequence. Two mechanisms are
implicated, both actionable. The backward budget (15 actions) cannot
populate a thirty-member index in one iteration, and nothing in the protocol
asks the agent to \emph{return} to an existing index and extend it, so
$43$ indexes were opened and abandoned. More fundamentally, the agent
indexes the key a question names rather than the class it belongs to: it
builds ``Friends of Delphine Grimsby'' when asked about Delphine Grimsby,
never ``friends, for everyone''. A protocol that scores an index by its
completeness over a class, and that rewards extending an existing index over
creating a new one, is the obvious next experiment; we state it as the
concrete next step rather than claim the hypothesis is settled.

\textbf{Deployment reading.} The abstraction targets a real configuration: agent
fleets maintaining shared repositories, wikis, and knowledge bases. Our failure
ledger maps directly onto production incidents---duplicated near-copies burying each
other is the lost-update problem; authored-without-provenance content is the
hallucinated fix; a deleted last instance is knowledge loss during refactoring. The
break-even point $N^{*}$ answers the deployment question in units operators use:
after how many queries does curation pay for itself. Our preliminary runs
(Sec.~\ref{sec:design}) double as a default-behavior audit: an agent given
a maintenance objective but no parsimony objective degraded the asset it was
tending while every individual action looked constructive---a caution for
unsupervised agent operation on shared state.

\textbf{Threats to validity.} Template-rendered language is simpler than natural
prose; phrasing regularity may flatter lexical matching (mitigated by embedding
retrieval and by the planned official-PhantomWiki arm, whose generator we do not
control). The reader and trainer share a model family, so structure tuned by the
trainer may suit that family's search habits. That family is also not the
current one: \texttt{gpt-5-mini-2025-08-07} was the small reasoning model
when these runs began, and several later generations exist. A stronger
reader is the harder test for us, not the easier one --- the better it is at
recovering a set by searching, the less an index can add --- so the effect
of re-running on a current model is to shrink whatever margin we report,
and we would expect the ordering among arms to survive rather than the
magnitudes. Single seed in the basic version;
single-agent by design (concurrent maintenance, with its conflict taxonomy, is the
natural sequel); support-set diagnostics are unavailable on the external arm.
Grading is token-F1 against short golds---adequate for entity answers, blunt for
free-form ones; the environment avoids the latter by construction.

\textbf{An online variant.} We deliberately separate a training phase from a
frozen-store examination, because that separation is what makes the
generalization question answerable at all: a store edited during evaluation
cannot be said to have been tested on anything. Nothing in the method
requires it. The training loop already answers a question before it edits,
so the same agent could curate against live traffic, treating whatever
feedback a deployment provides---a correction, a follow-up, a click---as the
label the gold answer supplies here. What such a variant would need is not a
different curator but a different measurement: with the store changing
underneath, held-out accuracy stops being well defined, and the honest
alternative is a prequential one, scoring each question before the edits it
provokes.

\begin{thebibliography}{00}
\bibitem{llmwiki-rar} Retrieval as reasoning: self-evolving agent-native retrieval
via LLM-wiki, arXiv:2605.25480, 2026.
\bibitem{llmwiki-stream} Streaming knowledge compilation: proactive
materiality-scored pinning for time-evolving LLM wikis, arXiv:2606.09877, 2026.
\bibitem{karpathy-llmwiki} A.~Karpathy, ``llm-wiki,'' public note, Apr.\ 2026.
\bibitem{graphrag} D.~Edge \emph{et al.}, ``From local to global: a GraphRAG approach
to query-focused summarization,'' arXiv:2404.16130, 2024.
\bibitem{raptor} P.~Sarthi \emph{et al.}, ``RAPTOR: recursive abstractive processing
for tree-organized retrieval,'' ICLR, 2024.
\bibitem{hipporag2} B.~Guti\'errez \emph{et al.}, ``From RAG to memory:
non-parametric continual learning for LLMs,'' ICML, 2025.
\bibitem{ace} ``Agentic context engineering: evolving contexts for self-improving
language models,'' ICLR, 2026.
\bibitem{phantomwiki} A.~Gong \emph{et al.}, ``PhantomWiki: on-demand datasets for
reasoning and retrieval evaluation,'' ICML, 2025.
\bibitem{ircot} H.~Trivedi \emph{et al.}, ``Interleaving retrieval with
chain-of-thought reasoning for knowledge-intensive multi-step questions,'' ACL, 2023.
\bibitem{selfrag} A.~Asai \emph{et al.}, ``Self-RAG: learning to retrieve, generate,
and critique through self-reflection,'' ICLR, 2024.
\bibitem{agentic-rag-survey} ``Reasoning RAG via system 1 or system 2: a survey on
reasoning agentic retrieval-augmented generation,'' arXiv:2506.10408, 2025.
\bibitem{amem} W.~Xu \emph{et al.}, ``A-MEM: agentic memory for LLM agents,''
arXiv:2502.12110, 2025.
\bibitem{longmemeval} D.~Wu \emph{et al.}, ``LongMemEval: benchmarking chat
assistants on long-term interactive memory,'' ICLR, 2025; and LongMemEval-V2,
arXiv:2605.12493, 2026.
\bibitem{memdelta} ``MemDelta: controlled baselines and hidden confounds in agent
memory evaluation,'' arXiv:2606.29914, 2026.
\bibitem{letta-sleep} Letta, ``Sleep-time compute,'' 2025.
\bibitem{rag-lewis} P.~Lewis \emph{et al.}, ``Retrieval-augmented generation
for knowledge-intensive NLP tasks,'' NeurIPS, 2020.
\bibitem{dpr} V.~Karpukhin \emph{et al.}, ``Dense passage retrieval for
open-domain question answering,'' EMNLP, 2020.
\bibitem{realm} K.~Guu \emph{et al.}, ``REALM: Retrieval-augmented language
model pre-training,'' ICML, 2020.
\bibitem{fid} G.~Izacard and E.~Grave, ``Leveraging passage retrieval with
generative models for open domain question answering,'' EACL, 2021.
\bibitem{react} S.~Yao \emph{et al.}, ``ReAct: Synergizing reasoning and
acting in language models,'' ICLR, 2023.
\bibitem{selfask} O.~Press \emph{et al.}, ``Measuring and narrowing the
compositionality gap in language models,'' Findings of EMNLP, 2023.
\bibitem{flare} Z.~Jiang \emph{et al.}, ``Active retrieval augmented
generation,'' EMNLP, 2023.
\bibitem{deeprag} X.~Guan \emph{et al.}, ``DeepRAG: Thinking to retrieval
step by step for large language models,'' arXiv:2502.01142, 2025.
\bibitem{searchr1} B.~Jin \emph{et al.}, ``Search-R1: Training LLMs to reason
and leverage search engines with reinforcement learning,'' arXiv:2503.09516,
2025.
\bibitem{memgpt} C.~Packer \emph{et al.}, ``MemGPT: Towards LLMs as operating
systems,'' arXiv:2310.08560, 2023.
\bibitem{mem0} P.~Chhikara \emph{et al.}, ``Mem0: Building production-ready
AI agents with scalable long-term memory,'' arXiv:2504.19413, 2025.
\bibitem{generative-agents} J.~S. Park \emph{et al.}, ``Generative agents:
Interactive simulacra of human behavior,'' UIST, 2023.
\bibitem{voyager} G.~Wang \emph{et al.}, ``Voyager: An open-ended embodied
agent with large language models,'' arXiv:2305.16291, 2023.
\bibitem{reflexion} N.~Shinn \emph{et al.}, ``Reflexion: Language agents with
verbal reinforcement learning,'' NeurIPS, 2023.
\bibitem{expel} A.~Zhao \emph{et al.}, ``ExpeL: LLM agents are experiential
learners,'' AAAI, 2024.
\bibitem{awm} Z.~Z. Wang \emph{et al.}, ``Agent workflow memory,''
arXiv:2409.07429, 2024.
\bibitem{cheatsheet} M.~Suzgun \emph{et al.}, ``Dynamic cheatsheet: Test-time
learning with adaptive memory,'' arXiv:2504.07952, 2025.
\bibitem{storm} Y.~Shao \emph{et al.}, ``Assisting in writing Wikipedia-like
articles from scratch with large language models,'' NAACL, 2024.
\bibitem{lightrag} Z.~Guo \emph{et al.}, ``LightRAG: Simple and fast
retrieval-augmented generation,'' arXiv:2410.05779, 2024.
\bibitem{hipporag} B.~Guti\'errez \emph{et al.}, ``HippoRAG:
Neurobiologically inspired long-term memory for large language models,''
NeurIPS, 2024.
\bibitem{hotpotqa} Z.~Yang \emph{et al.}, ``HotpotQA: A dataset for diverse,
explainable multi-hop question answering,'' EMNLP, 2018.
\bibitem{musique} H.~Trivedi \emph{et al.}, ``MuSiQue: Multihop questions via
single-hop question composition,'' TACL, 2022.
\bibitem{twowiki} X.~Ho \emph{et al.}, ``Constructing a multi-hop QA dataset
for comprehensive evaluation of reasoning steps,'' COLING, 2020.
\bibitem{squad} P.~Rajpurkar \emph{et al.}, ``SQuAD: 100,000+ questions for
machine comprehension of text,'' EMNLP, 2016.
\bibitem{locomo} A.~Maharana \emph{et al.}, ``Evaluating very long-term
conversational memory of LLM agents,'' ACL, 2024.
\bibitem{rome} K.~Meng \emph{et al.}, ``Locating and editing factual
associations in GPT,'' NeurIPS, 2022.
\bibitem{memit} K.~Meng \emph{et al.}, ``Mass-editing memory in a
transformer,'' ICLR, 2023.
\bibitem{star} E.~Zelikman \emph{et al.}, ``STaR: Bootstrapping reasoning
with reasoning,'' NeurIPS, 2022.
\end{thebibliography}

\end{document}
